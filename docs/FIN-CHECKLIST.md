# FIN — единый трекер маршрута завершения

Обновлено: 2026-09-11. Первичный план: [PLAN-FIN-RU.md](PLAN-FIN-RU.md) (единственный актуальный указатель; этот файл — его состояние).
Состояния: `TODO` · `IN_PROGRESS` · `LOCAL_PASS` · `HOSTED_PASS` · `UI_PASS` · `PARTIAL` · `BLOCKED`.

| Карточка | Исходные | Статус | Доказательство | Следующее действие |
|---|---|---|---|---|
| FIN-00 контекст и карта | — | LOCAL_PASS | этот файл + SHA ниже | — |
| FIN-01 неизменяемый bundle и rollback | B02, M01/M06 | PARTIAL | первый проход: `fin01-release-bundle.test.mjs` 5/5 (до фикса 0/5); pin `releaseId → bundle`, preflight rollback (`1886041`). Второй проход (независимая adversarial-проверка нашла 3 живые дыры **публичного** контура): сборка замораживает пин **до** создания релиза и отвечает `422 RELEASE_FREEZE_FAILED`, если не может; пин фиксирует ревизию **на момент сборки**, а не «текущий черновик» при публикации; создание публичной сессии сверяет манифест → `409 PUBLIC_MISSION_ASSET_CHANGED` при дрейфе; adopted legacy-пин сохраняет уже объявленный `contentHash`. Нормативный контракт `fin01-release-contract.test.mjs` 4/4 (3 из 4 RED до фикса); характеризующий набор `fin01-verify-*` остаётся в `origin/feat/fin01-adversarial-verify` | гонки между `pinRelease` и сдвигом указателя — не одна транзакция; миграция реальной дофиксовой БД не гонялась на стенде; hosted-прогон этих путей |
| FIN-02 согласованная публикация/повтор | B03, M06 | LOCAL_PASS | `apps/server/test/fin02-publication-atomicity.test.mjs` 3/3 (до фикса — 1/3: `published` вместо `unlisted`); staged-операции в Memory и SQLite; прерванная публикация settles при старте сервера (`settleInterruptedPublications`) — `apps/server/test/fin02-publication-recovery.test.mjs` 2/2 (до правки RED 0/2: перезапуск оставлял указатель и каталог врозь, а `pending`-операцию никто не доводил); набор гоняется на файловой SQLite (`SQLiteControlPublicationStore` + `SQLiteControlReleaseStore`), а не на MemoryStore; Server 157/157 (155 + 2 новых), Contracts 57/57 | hosted-проверка после доставки: реальный `docker compose restart` на VPS |
| FIN-03 старые сессии сохраняют ассеты | B04, M03/M06 | PARTIAL | engine-половина: session-pinned маршрут `/public/v1/missions/:id/sessions/:sid/assets/:assetId` отдаёт байты ревизии, к которой привязана сессия; и публичный, и session-маршрут отдают байты **pinned digest** релиза (`getReleasePin(...).assets[].hash`), а не текущую запись библиотеки — повторная загрузка того же assetId больше не подменяет байты под `immutable`. Credential сессии обязателен (нет/подделка → 401, чужая сессия/миссия → 404), отдача `private`; `apps/server/test/fin03-session-assets.test.mjs` 3/3 (RED 2/3 до правки pinned-байтов); независимый репро-набор субагента `fin03-open-session-assets.test.mjs` (3 кейса: перезагрузка байтов под immutable-URL, republish, unpublish) — RED `3 fail` до фикса, **3/3** на ветке; набор сохранён в ветке (Server 147/147). site-половина — `feat/fin03-site-session-assets` (`6eb4f9c`, отдельный репозиторий) | hosted-прогон через nginx под gate |
| FIN-04 доставка всех компонентов | B01, R04 | PARTIAL | engine exact-SHA `7e61f98` + supervised `lhc-authored` доставлены (2026-09-11); version-манифест состава — `deploy/vps/delivery-manifest.json` (генерируется `delivery-manifest.mjs` из commit + SHA-256 файла gate), тест `apps/server/test/fin04-delivery-manifest.test.mjs` 2/2 (RED до манифеста 1/2) | B01 закрыт частично: манифест называет компоненты и писателей общей БД, но runtime-детекции «старый Control пишет в общую БД» нет; манифест — снимок доставленного SHA, после следующей доставки его надо перегенерировать |
| FIN-05 экраны и ручная композиция | M03–M05, F05 | PARTIAL | доска сюжета: сцены/выборы create·rename·duplicate·delete, подписи стрелок, undo/redo + клавиатура, «Вписать всё» и zoom к курсору, layout≠contentHash — `fin05-story-board.test.mjs` 15/15. Композиция экрана (FIN-05B, влита): слои drag/resize(пропорц.)/rotate/flip/opacity/z-order/duplicate/delete/lock/visible, contain/cover + фокус и crop, наследование фона (own/inherited/none), preset + reduced-motion/пауза, mute/play/blocked для музыки, клавиатура (стрелки/Shift, [ ] { }, V, L, D, Del, Esc), запись через тот же CAS `/mission` — `fin05b-screen-composition.test.mjs` 16/16; `test:studio` 142/0/1; Control 106/106, Server 141/141, Contracts 57/57 | реальная библиотека материалов (hash/MIME/размеры, restart/другой браузер), экраны intro/scene/dialogue/choice/ending в реальном runtime/BFF, browser-приёмка (3+ сцены, 2 ветви, 2 финала не-Florence) |
| FIN-06 ручной маршрут и права | M06, C18 | LOCAL_PASS (права) / PARTIAL (живые роли) | capability-матрица `docs/CAPABILITY-MATRIX.md`; `apps/server/test/fin06-role-matrix-http.test.mjs` 3/3 (13 разрешённых, 20 отказных на живом HTTP с четырьмя локальными identities); Server 160/160, typecheck 0; hosted happy-path пройден ранее | живые editor/viewer/read-only сессии по приглашениям (C18), привязка прав к подтверждённому numeric Telegram ID |
| FIN-07 подключения провайдеров | M07.a | TODO | — | «Настройки → ИИ», защищённое хранение ключей |
| FIN-08 каталог моделей и router | M07.b | TODO | — | профили автор/ведущий, live inference отдельно от списка |
| FIN-09 ИИ создаёт полную миссию | M08 | TODO | — | writer + intent-контракт ведущего, две ветви и два финала |
| FIN-10 понятность для новичка | M09 | TODO | — | тур по реальным элементам, русские ошибки с действием |
| FIN-11 сквозная приёмка авторства | M10 | TODO | — | одна воспроизводимая история проверки + E-матрица |
| FIN-12 стикеры и комментарии | V07 | PARTIAL | серверная часть (влита): хранилище заметок/тредов с append-only ревизиями, CAS-конфликтами (`409 COLLABORATION_REVISION_CONFLICT`), мягким tombstone, авторством и ролями, плюс HTTP-маршруты (401/403/404, CSRF + idempotency-key, replay). Тесты: `collaboration-store.test.mjs` + `collaboration-http.test.mjs` — 10/10 (7 новых); несущая способность доказана 6 временными мутациями исходников (каждая красила конкретные тесты, мутации откачены). Оговорки: нет message-level parent и CAS на ответах, повторная установка того же статуса — no-op с бампом ревизии, `MemoryControlStore` и ветка `501 COLLABORATION_STORAGE_UNAVAILABLE` не покрыты | UI влит `b20d72b`: вкладка «Заметки» в Studio (`apps/studio/src/collaboration.ts` + `api.ts`/`app.ts`/`styles.css`), 10 тестов `fin12-notes-ui.test.mjs` (read-only без write-контролов, 409-конфликт с сохранением локального текста, экранирование и запрет обрезки, кросс-граничный E2E через прокси Studio к живому SQLite Control). Несущая способность доказана 3 временными мутациями (снятие read-only guard, ветка 409, `text-overflow: ellipsis`) — каждая красила конкретные тесты, мутации откачены. Осталось: живой браузерный прогон вкладки, пин на доске (показываются только координаты), живые роли C18 |
| FIN-13 совместная доска | V08 | TODO | — | presence + конкурентное сохранение |
| FIN-14 финальная приёмка и передача | — | TODO | — | пакет передачи: адреса, 2 миссии, инструкции, manifest |

Четыре подтверждённые проблемы повторного аудита M06 и их карточки: B01 → FIN-04, B02 → FIN-01, B03 → FIN-02, B04 → FIN-03.

## Текущее состояние (SHA)

- Engine `C:/Users/kato55/Documents/Codex/2026-09-09-live-author-studio`, ветка `feat/b13-acceptance-closure`, вход `97810ea`.
- Site `C:/Temp/lhc-site-audit-20260910`, ветка `feat/florence-vertical-slice`, `db7cbe848…` (checkout не переключать).
- VPS `/opt/lhc/engine` — `7e61f9889724f01a5f5c1c2e511093c177ceac6b`; hosted-фиксы FIN-01 на стенде **не развёрнуты**.
- Runtime проверок: portable Node `24.19.0` (`AppData/Local/Temp/node-v24.19.0-win-x64`); системный `v22.23.2` даёт ложный fail `C17`.

## Не проверено / блокеры

- FIN-01: hosted-проверка rollback требует доставки новой ревизии engine на стенд (не выполнялось в этой карточке).
- FIN-02: невидимость кандидата до commit, CAS/idempotent-повтор и восстановление после падения между стадиями закрыты тестами (`fin02-publication-atomicity` 3/3, `fin02-publication-recovery` 2/2 на файловой SQLite); сервер до первого запроса закрывает прерванную операцию по факту — commit, если указатель уже назвал целевой релиз, иначе abort (`docs/worklog/2026-09-11-FIN02-publication-recovery.md`). Не проверено: реальный перезапуск процесса на стенде (нужна доставка новой ревизии engine) и миграция настоящей дофиксовой БД (пока только фикстуры).
- FIN-01 известное ограничение: pin фиксируется при **сборке** релиза (HTTP-маршрут build, до регистрации релиза — при невозможности заморозить сборка отвечает `422 RELEASE_FREEZE_FAILED`). Если pin отсутствует (релиз собран в обход HTTP-маршрута), публикация/rollback берут ревизию, доказанную записью публикации; иначе — новейшую ревизию на момент обращения (best-effort) либо `LEGACY_PIN_UNPROVABLE` для rollback. Pin и сдвиг указателя остаются отдельными записями, но расхождение «указатель сдвинут — каталог нет» больше не переживает старт: `settleInterruptedPublications` закрывает прерванную операцию до первого запроса (`fin02-publication-recovery` 2/2). Миграция реальной дофиксовой БД на стенде не гонялась.
- FIN-06: матрица прав и backend negative tests на локальных identities закрыты (`apps/server/test/fin06-role-matrix-http.test.mjs` 3/3, `docs/CAPABILITY-MATRIX.md`, несущая способность проверена двумя мутациями с откатом); живые роли C18 требуют приглашённых Telegram-аккаунтов и не имитируются; gate не ослаблять. Открытые решения владельца: отдельная роль `viewer` (сейчас read-only = `tester`) и право комментировать для read-only участника.
- FIN-05: браузерный проход gated Studio с реальным входом не выполнялся (gate не ослаблялся, credentials не подбирались); есть regression-тест dev:studio с настоящим Control и DOM-шим-тесты `story-dom.ts`/`screen-dom.ts`. Композиция экрана закрыта на уровне модели, DOM-сцены и записи через `/mission`, но **не** пройдена в живом браузере, не подключена к реальной библиотеке материалов (hash/MIME/размеры, переживание restart/другого браузера) и не воспроизведена в Player/сайте (site-половина сделана в отдельном репозитории, ветка `feat/fin03-site-session-assets` `ed27170`: фон own/inherited/none, preset анимации, музыка экрана, mute/автоплей; 21 файл / 109 тестов, `check` и `build` exit 0 — живой браузерный прогон не делался; диалоги сцены и пагинация нескольких интро — ветка `feat/fin05-site-dialogue` `fea316b`: строки диалога листаются по клику/тапу и клавишам (Ctrl/Meta/Alt и клавиша на самоактивирующемся контроле не перехватываются), «Далее»/«Начать» на последней странице интро, позиция переживает перерисовку кадра (идентичность последовательности — по авторскому контенту, не по объекту), один ввод продвигает не более чем на шаг, 23 файла / 129 тестов, `check` и `build` exit 0; несущая способность — 3 временные мутации, мутации откачены); приёмка 3+ сцен/2 финалов не-Florence не достигнута (см. `docs/worklog/2026-09-11-FIN05B-screen-composition.md`).
- FIN-04: production-канал и рестарт gate/блога/Ивы без явного разрешения владельца не выполняются.
