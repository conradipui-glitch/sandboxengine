# FIN — единый трекер маршрута завершения

Обновлено: 2026-09-11. Первичный план: [PLAN-FIN-RU.md](PLAN-FIN-RU.md) (единственный актуальный указатель; этот файл — его состояние).
Состояния: `TODO` · `IN_PROGRESS` · `LOCAL_PASS` · `HOSTED_PASS` · `UI_PASS` · `PARTIAL` · `BLOCKED`.

| Карточка | Исходные | Статус | Доказательство | Следующее действие |
|---|---|---|---|---|
| FIN-00 контекст и карта | — | LOCAL_PASS | этот файл + SHA ниже | — |
| FIN-01 неизменяемый bundle и rollback | B02, M01/M06 | PARTIAL | первый проход: `fin01-release-bundle.test.mjs` 5/5 (до фикса 0/5); pin `releaseId → bundle`, preflight rollback (`1886041`). Второй проход (независимая adversarial-проверка нашла 3 живые дыры **публичного** контура): сборка замораживает пин **до** создания релиза и отвечает `422 RELEASE_FREEZE_FAILED`, если не может; пин фиксирует ревизию **на момент сборки**, а не «текущий черновик» при публикации; создание публичной сессии сверяет манифест → `409 PUBLIC_MISSION_ASSET_CHANGED` при дрейфе; adopted legacy-пин сохраняет уже объявленный `contentHash`. Нормативный контракт `fin01-release-contract.test.mjs` 4/4 (3 из 4 RED до фикса); характеризующий набор `fin01-verify-*` остаётся в `origin/feat/fin01-adversarial-verify`. Третий проход (миграция на реальном ФАЙЛЕ дофиксовой схемы, `feat/fin01-legacy-migr`): `packages/control/test/fin01-legacy-file-migration.test.mjs` 5/5 и `apps/server/test/fin01-legacy-file-migration.test.mjs` 1/1 (+1 skip) на файловой БД, а не на фикстурах в памяти; открытие сторов добавляет ровно `control_publication_release_pins`, `control_publication_pin_idempotency`, `control_publication_operations` (+индекс) и не переписывает строки; adoption доказывает **записанную** ревизию (`assetsVerified:false`), rollback к недоказуемому релизу — `409 LEGACY_PIN_UNPROVABLE` без сдвига каталога и указателя; `scripts/fin01-legacy-migration.mjs` (`npm run migrate:fin01`) делает verified backup (online backup API + `integrity_check`), миграцию и JSON-отчёт по неразрешимым записям, exit 2 когда нужен оператор | гонки между `pinRelease` и сдвигом указателя — не одна транзакция; hosted-прогон этих путей; публикация релиза **без** пина и без называющей записи публикации по-прежнему подставляет новейший draft как `assetsVerified:true` (правка в `control-server.ts`, вне поверхности этой карточки — тест помечен skip, инструмент миграции такой пин не пишет) **Четвёртый проход (эта волна): подстановка нового черновика в недоказуемый релиз закрыта.** `resolveReleaseBundle` больше не берёт новейшую авторскую ревизию для релиза без пина: запись релиза хранит ревизию и хэш **доски** (`control_releases.draft_revision`), а не авторской ревизии документа миссии, поэтому доказать содержание такого релиза нечем — публикация и откат честно отвечают `409 PUBLICATION_BUNDLE_UNAVAILABLE` с `detailCode: LEGACY_PIN_UNPROVABLE`, а `assetsVerified: true` из воздуха не выставляется. Доказуемый путь один — заморозка при сборке (HTTP-маршрут build), и он проверен тестом на то, что правка черновика после сборки не меняет опубликованное содержание. Воспроизводящие тесты: `apps/server/test/fin02-publication-consistency.test.mjs` (5/5; до правки 2 из 3 сценариев красные), ранее skip-нутый тест `fin01-legacy-file-migration.test.mjs` включён и проходит. Несущая способность: мутация M1 (возврат подстановки новейшего черновика) → красный прогон, откат → зелёный. |
| FIN-02 согласованная публикация/повтор | B03, M06 | LOCAL_PASS | `apps/server/test/fin02-publication-atomicity.test.mjs` 3/3 (до фикса — 1/3: `published` вместо `unlisted`); staged-операции в Memory и SQLite; прерванная публикация settles при старте сервера (`settleInterruptedPublications`) — `apps/server/test/fin02-publication-recovery.test.mjs` 2/2 (до правки RED 0/2: перезапуск оставлял указатель и каталог врозь, а `pending`-операцию никто не доводил); набор гоняется на файловой SQLite (`SQLiteControlPublicationStore` + `SQLiteControlReleaseStore`), а не на MemoryStore; Server 157/157 (155 + 2 новых), Contracts 57/57 | hosted-проверка после доставки: реальный `docker compose restart` на VPS **Пятый проход (эта волна): согласованность конкурентных публикаций.** Коммит операции сверяется с живым указателем релиза **внутри транзакции** (`BEGIN IMMEDIATE`) в обоих хранилищах: если указатель уже назвал другой релиз, кандидат помечается `aborted` и возвращается `stale_candidate`, каталог не меняется, сервер отвечает `409 PUBLICATION_CANDIDATE_STALE`. Хранилище в памяти получает живой указатель от вызывающего и отвергает устаревший кандидат тем же кодом. Тесты там же: 5/5 (устаревший кандидат после сдвига указателя; параллельные публикации — каталог обязан совпадать с указателем релиза после каждой гонки; ветка хранилища в памяти). Несущая способность: мутация M2 (снятие сверки с указателем в сторе) → красный прогон, откат → зелёный. Остаток: параметр `expectedCurrentReleaseId` на сервере для SQLite-стора мутацией не краснит — стор читает указатель сам. |
| FIN-03 старые сессии сохраняют ассеты | B04, M03/M06 | PARTIAL | engine-половина: session-pinned маршрут `/public/v1/missions/:id/sessions/:sid/assets/:assetId` отдаёт байты ревизии, к которой привязана сессия; и публичный, и session-маршрут отдают байты **pinned digest** релиза (`getReleasePin(...).assets[].hash`), а не текущую запись библиотеки — повторная загрузка того же assetId больше не подменяет байты под `immutable`. Credential сессии обязателен (нет/подделка → 401, чужая сессия/миссия → 404), отдача `private`; `apps/server/test/fin03-session-assets.test.mjs` 3/3 (RED 2/3 до правки pinned-байтов); независимый репро-набор субагента `fin03-open-session-assets.test.mjs` (3 кейса: перезагрузка байтов под immutable-URL, republish, unpublish) — RED `3 fail` до фикса, **3/3** на ветке; набор сохранён в ветке (Server 147/147). site-половина — `feat/fin03-site-session-assets` (`6eb4f9c`, отдельный репозиторий) | hosted-прогон через nginx под gate |
| FIN-04 доставка всех компонентов | B01, R04 | PARTIAL | engine exact-SHA `7e61f98` + supervised `lhc-authored` доставлены (2026-09-11); version-манифест состава — `deploy/vps/delivery-manifest.json` (генерируется `delivery-manifest.mjs` из commit + SHA-256 файла gate), тест `apps/server/test/fin04-delivery-manifest.test.mjs` 2/2 (RED до манифеста 1/2) | B01 закрыт частично: манифест называет компоненты и писателей общей БД, но runtime-детекции «старый Control пишет в общую БД» нет; манифест — снимок доставленного SHA, после следующей доставки его надо перегенерировать |
| FIN-05 экраны и ручная композиция | M03–M05, F05 | PARTIAL | доска сюжета: сцены/выборы create·rename·duplicate·delete, подписи стрелок, undo/redo + клавиатура, «Вписать всё» и zoom к курсору, layout≠contentHash — `fin05-story-board.test.mjs` 15/15. Композиция экрана (FIN-05B, влита): слои drag/resize(пропорц.)/rotate/flip/opacity/z-order/duplicate/delete/lock/visible, contain/cover + фокус и crop, наследование фона (own/inherited/none), preset + reduced-motion/пауза, mute/play/blocked для музыки, клавиатура (стрелки/Shift, [ ] { }, V, L, D, Del, Esc), запись через тот же CAS `/mission` — `fin05b-screen-composition.test.mjs` 16/16; `test:studio` 142/0/1; Control 106/106, Server 141/141, Contracts 57/57 | реальная библиотека материалов (hash/MIME/размеры, restart/другой браузер), экраны intro/scene/dialogue/choice/ending в реальном runtime/BFF, browser-приёмка (3+ сцены, 2 ветви, 2 финала не-Florence) |
| FIN-06 ручной маршрут и права | M06, C18 | LOCAL_PASS (права) / PARTIAL (живые роли) | capability-матрица `docs/CAPABILITY-MATRIX.md`; `apps/server/test/fin06-role-matrix-http.test.mjs` 3/3 (13 разрешённых, 20 отказных на живом HTTP с четырьмя локальными identities); Server 160/160, typecheck 0; hosted happy-path пройден ранее | живые editor/viewer/read-only сессии по приглашениям (C18), привязка прав к подтверждённому numeric Telegram ID |
| FIN-07 подключения провайдеров | M07.a | TODO | LOCAL_PASS (живая проверка) / PARTIAL (UI каталога) | Хранилище подключений `packages/control/src/provider-connections-store.ts` (14 тестов, утечка ключа в ответ — под мутацией) + панель `apps/studio/src/provider-connections-panel.ts` (14 тестов, не смонтирована). Серверная проводка оркестратора: ключ лежит в локальном файле стенда, наружу отдаётся только маска (`credentialStorage: local_file_masked`, `credentialMask: sk-…424b`), `revealApiKey` — только для исходящего запроса; отдельная проверка соединения `POST /local/author-provider/probe`; отключение стирает секрет (`clearConnection`, тест переживания перезапуска). **Живая проверка**: `state: connected` через OpenRouter, после перезапуска процесса подключение восстановлено из файла (`hasCredential: true`, маска та же), ключ ни в одном ответе не появился. | смонтировать панель каталога в UI (сейчас работает существующая форма настроек); живая проверка ошибок (401/429) на реальном провайдере |
| FIN-08 каталог моделей и router | M07.b | TODO | LOCAL_PASS (модули) / TODO (в интерфейсе) | `packages/ai/src/model-catalog.ts` + `profile-router.ts` — 17 тестов (детерминизм, недоступный провайдер, пустой каталог, заморозка данных); `apps/studio/src/model-picker.ts` — 15 тестов (рекомендация первой, две альтернативы, честный пустой случай, ключ не попадает в DOM). Модули влиты, сборка зелёная. | выбор модели в интерфейсе: панель готова, но не смонтирована в app.ts (оркестратор не успел) — иначе карточка закрыта |
| FIN-09 ИИ создаёт полную миссию | M08 | TODO | LOCAL_PASS (живая генерация) | `packages/ai/src/mission-writer.ts` (22 теста) + детерминированные починки оркестратора: дубли идентификаторов сцен и висячие цели выборов (цель ведёт в финал своей ветви; факт починки попадает в `repairs`, содержание не выдумывается). HTTP-проводка `POST /local/mission-draft` (тот же провайдер, что в настройках), кнопка «Создать с ИИ» в Studio просит полную миссию и сохраняет документ; отказ ИИ описывается словами и не подменяется пустым черновиком. **Живая проверка (OpenRouter)**: сгенерирована миссия — 9 сцен, 2 финала, 18 выборов, 4 детерминированные починки; документ сохранён (ревизия 1) и **повторно открыт** с теми же 9 сценами. | живой прогон именно через кнопку в браузере (API-путь проверен); прогон на другой модели/жанре |
| FIN-10 понятность для новичка | M09 | TODO | LOCAL_PASS (модуль + монтирование) / PARTIAL (живой браузер) | `apps/studio/src/onboarding-tour.ts` — 25 тестов (10 шагов по реальным якорям app.ts/index.html, пропуск неприменимого шага с причиной, состояния и возобновление, 30 кодов ошибок сервера с действием, запрет обрезки многоточием). Оркестратор смонтировал: кнопка «Тур по Studio» в меню редактора, шаги с прогрессом через адаптер `PreferenceStore`, объяснение ошибок действием в `setError` (к описанию кода добавляется что сделать). | живой проход тура в браузере и проверка, что якоря находятся в реальной разметке (в тестах сверено с исходниками) |
| FIN-11 сквозная приёмка авторства | M10 | TODO | LOCAL_PASS | `apps/server/test/fin11-authoring-journey.test.mjs` — одна история на живом HTTP: проект → миссия → доска → документ с двумя ветвями и двумя финалами → экраны → валидация → релиз → публикация → каталог → сессия → два хода по разным ветвям → два финала → повторный ход после финала отвергается. Плюс `docs/E-MATRIX.md`: шаг → маршрут → что доказывает → имя теста → что не покрыто. | живой браузерный проход по тому же маршруту (скрипт `scripts/live-acceptance/new-mission-journey.mjs` готов, запускается локально) |
| FIN-12 стикеры и комментарии | V07 | PARTIAL | PARTIAL | Сервер: ответы на конкретное сообщение (`parent_message_id`, проверка родителя), CAS на ответах, честный no-op при повторной установке статуса, миграция колонки на старой схеме — отдельный набор тестов. **Важно:** политика «ответ на ответ — плоская привязка к корню, CAS на ответах обязателен» реализована модулем `collaboration-replies.ts` (7 тестов, мутации M1–M4), но в производственном пути не включена: нужна одна строка композиции `collaborationStore: withReplyPolicy(store)` (поле в `ControlServerDependencies` уже есть). Studio: `apps/studio/src/board-pins.ts` — пины-привязки к узлам/сценам с маркерами, веером без наложения и отсевом удалённых якорей (тесты, модуль влит). | монтирование пинов на доске и живой прогон обсуждения в браузере |
| FIN-13 совместная доска | V08 | TODO | LOCAL_PASS (сервер) / TODO (UI) | `packages/control/src/board-presence-store.ts` — 29 тестов: присутствие с пульсом и TTL, память + SQLite (собственная таблица), конкурентное сохранение доски с ревизией и конфликтом на двух соединениях, идемпотентность. | HTTP-маршруты и индикация участников в Studio (стор готов, проводки нет) |
| FIN-14 финальная приёмка и передача | — | TODO | LOCAL_PASS (пакет) | `scripts/handoff-package.mjs` + `deploy/vps/handoff-manifest.json` + `docs/TEAM-HANDOFF.md` — пакет передачи: адреса, состав, роли, две демонстрационные миссии, инструкции; тесты на отсутствие секретов, детерминизм и pending-поля. | наполнить манифест фактическими id двух миссий командного стенда после доставки |

Четыре подтверждённые проблемы повторного аудита M06 и их карточки: B01 → FIN-04, B02 → FIN-01, B03 → FIN-02, B04 → FIN-03.

## Текущее состояние (SHA)

- Engine `C:/Users/kato55/Documents/Codex/2026-09-09-live-author-studio`, ветка `feat/b13-acceptance-closure`, вход `97810ea`.
- Site `C:/Temp/lhc-site-audit-20260910`, ветка `feat/florence-vertical-slice`, `db7cbe848…` (checkout не переключать).
- VPS `/opt/lhc/engine` — `7e61f9889724f01a5f5c1c2e511093c177ceac6b`; hosted-фиксы FIN-01 на стенде **не развёрнуты**.
- Runtime проверок: portable Node `24.19.0` (`AppData/Local/Temp/node-v24.19.0-win-x64`); системный `v22.23.2` даёт ложный fail `C17`.

## Не проверено / блокеры

- FIN-01: hosted-проверка rollback требует доставки новой ревизии engine на стенд (не выполнялось в этой карточке).
- FIN-02: невидимость кандидата до commit, CAS/idempotent-повтор и восстановление после падения между стадиями закрыты тестами (`fin02-publication-atomicity` 3/3, `fin02-publication-recovery` 2/2 на файловой SQLite); сервер до первого запроса закрывает прерванную операцию по факту — commit, если указатель уже назвал целевой релиз, иначе abort (`docs/worklog/2026-09-11-FIN02-publication-recovery.md`). Не проверено: реальный перезапуск процесса на стенде (нужна доставка новой ревизии engine) и миграция настоящей дофиксовой БД (пока только фикстуры).
- FIN-01 (обновлено этой волной): pin фиксируется при **сборке** релиза (HTTP-маршрут build; при невозможности заморозить сборка отвечает `422 RELEASE_FREEZE_FAILED`). Если pin отсутствует и релиз не назван записью публикации, публикация и rollback теперь **отвергаются** (`LEGACY_PIN_UNPROVABLE`) — новейший черновик не подставляется ни в каком случае. Расхождение «указатель сдвинут — каталог нет» не переживает старт (`settleInterruptedPublications`), а расхождение от гонки двух публикаций закрыто сверкой с указателем внутри транзакции коммита. Остаётся: заморозка и сдвиг указателя — по-прежнему две записи (одной транзакции нет), миграция реальной дофиксовой БД на стенде не гонялась, hosted-прогон этих путей не делался.
- FIN-06: матрица прав и backend negative tests на локальных identities закрыты (`apps/server/test/fin06-role-matrix-http.test.mjs` 3/3, `docs/CAPABILITY-MATRIX.md`, несущая способность проверена двумя мутациями с откатом); живые роли C18 требуют приглашённых Telegram-аккаунтов и не имитируются; gate не ослаблять. Открытые решения владельца: отдельная роль `viewer` (сейчас read-only = `tester`) и право комментировать для read-only участника.
- FIN-05: браузерный проход gated Studio с реальным входом не выполнялся (gate не ослаблялся, credentials не подбирались); есть regression-тест dev:studio с настоящим Control и DOM-шим-тесты `story-dom.ts`/`screen-dom.ts`. Композиция экрана закрыта на уровне модели, DOM-сцены и записи через `/mission`, но **не** пройдена в живом браузере, не подключена к реальной библиотеке материалов (hash/MIME/размеры, переживание restart/другого браузера) и не воспроизведена в Player/сайте (site-половина сделана в отдельном репозитории, ветка `feat/fin03-site-session-assets` `ed27170`: фон own/inherited/none, preset анимации, музыка экрана, mute/автоплей; 21 файл / 109 тестов, `check` и `build` exit 0 — живой браузерный прогон не делался; диалоги сцены и пагинация нескольких интро — ветка `feat/fin05-site-dialogue` `fea316b`: строки диалога листаются по клику/тапу и клавишам (Ctrl/Meta/Alt и клавиша на самоактивирующемся контроле не перехватываются), «Далее»/«Начать» на последней странице интро, позиция переживает перерисовку кадра (идентичность последовательности — по авторскому контенту, не по объекту), один ввод продвигает не более чем на шаг, 23 файла / 129 тестов, `check` и `build` exit 0; несущая способность — 3 временные мутации, мутации откачены); приёмка 3+ сцен/2 финалов не-Florence не достигнута (см. `docs/worklog/2026-09-11-FIN05B-screen-composition.md`).
- FIN-04: production-канал и рестарт gate/блога/Ивы без явного разрешения владельца не выполняются.
