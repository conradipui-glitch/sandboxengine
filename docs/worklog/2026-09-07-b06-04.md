# 2026-09-07 — B06-04 AgentBackend + live eval + canonical B06 audit

## Base

Начато только после published B06-03:

- B06-03 merge: `a21e7cb9c19b873049bb941d34e0482d6c45568d`;
- main push-CI: `34081046917` — success;
- branch: `b06-04-agent-backend-live-eval-b06-audit`;
- PR: #23.

## Цель slice

Закрыть B06 как целостный Runtime AI subsystem:

- отделить session-oriented AgentBackend от stateless ModelProvider;
- проверить current Codex compatibility без принудительной production интеграции;
- добавить explicit bounded live eval;
- провести canonical B06 audit и исправить только реальные BLOCKER.

## AgentBackend boundary

Commits `19ef8e3a1875a3c814300bd0c18b2629724480c5` / `335a393e92855985ddf49bc8b866f43b67e248dd` / `a9338a9556513a9e116714111f4323c4334603be`:

- отдельный `AgentBackend` contract;
- explicit open/runTurn/close lifecycle;
- normalized auth/rate-limit/session-expired/timeout/abort/backend errors;
- server-side opaque session handle;
- observed usage/request IDs;
- hard no-tools safe capabilities;
- no shell/filesystem/code/repository/external tools;
- no gameplay mutation/commit API;
- deterministic fake + lifecycle/safety tests.

Contract gate CI `34081275103` на `a9338a9556513a9e116714111f4323c4334603be` — **success**.

## Codex compatibility spike

Проверен current upstream `openai/codex` main snapshot `121f91fd5d9dc66017866ce9bdc49f1e182721df`.

Подтверждено:

- session/thread lifecycle;
- API-key + ChatGPT/browser/device login flows;
- thread start/resume/turn APIs;
- sandbox controls.

Но current supported sandbox semantics (`read_only`/workspace/full) не дают доказанного `toolPolicy:none`: read-only всё равно разрешает filesystem reads, а stable global all-built-in-tools-off guarantee не подтверждён.

Verdict зафиксирован в `docs/spikes/2026-09-07-codex-agent-backend-compatibility.md`:

`limited / BUILTIN_TOOLS_CANNOT_BE_PROVEN_ABSENT`

Production Codex Runtime adapter **не добавлен**.

## Live eval harness

Commits `bc5091da439c24b322fb356fbcc06f5493b4ee2d`, `aee6d233ebf043dfd1486902a51bc210b1c17d23`, `bc94fb98bddb36adc98abca929959da53b4355a9`:

- explicit `npm run eval:ai`;
- configured via `LHE_EVAL_API_KEY`, `LHE_EVAL_MODEL`, optional base URL/output path;
- not part of root deterministic `verify`;
- no key/model → clean `status: not_configured`, exit 0;
- intent corpus: positive paint + T02/T04/T09/T16 + unsupported;
- narrator corpus: executed/partial/blocked strict + expressive;
- separate `contractPass` and `semanticPass`;
- latency/attempts/observed usage/model/request IDs;
- unknown usage stays null;
- result schema does not contain credential;
- regression checks unrelated secret sentinel is not echoed.

AgentBackend + eval implementation head `bc94fb98bddb36adc98abca929959da53b4355a9` прошёл full CI `34081452303` — **success**.

## Live credential state

Current repository/assistant tool boundary does not expose GitHub secrets and B06-04 intentionally does not attempt to read/guess them.

Therefore no claim is made that a real provider was evaluated during deterministic CI. When explicit `LHE_EVAL_*` credential/model are supplied by an operator, the harness can perform the bounded live run.

Absence of credential = `not_configured`, classified as honest limitation, not failure of deterministic architecture.

## Canonical B06 audit

Created `docs/audits/2026-09-07-b06-canonical-audit.md`.

Reviewed:

- authority;
- secret/egress boundaries;
- deadlines/retries;
- claim/idempotency/single commit;
- quota/usage semantics;
- Player/public diagnostics;
- AgentBackend no-tools boundary;
- live semantic-quality separation;
- B07 scope leakage.

Audit result before publication docs gate:

- unresolved `BLOCKER`: **0**;
- FOLLOW_UP: future AgentBackend output must re-enter strict intent/narrator consumers; deployment DNS-aware egress; operator live eval; concrete agent quota only after backend acceptance;
- LIMITATION: Codex current no-tools incompatibility, external SLA, fake tests != absolute language correctness.

No B06 code closure gap requiring another implementation commit was found after green implementation CI.

## ADR

ADR 0021: `docs/decisions/0021-agent-backend-live-eval-and-b06-closure.md`.

Key decision:

- AgentBackend remains separate;
- no-tools is mandatory for Runtime use;
- Codex current verdict limited;
- live eval remains explicit/operator-configured;
- canonical B06 closure requires zero unresolved blocker + publication gate.

## Publication Gate

Remaining:

1. STATUS/HANDOFF sync;
2. final current-head PR CI;
3. PR #23 mark ready;
4. merge with pinned expected head;
5. exact merge-SHA push-to-main CI;
6. only then B06-04 published and canonical B06 closed.

Expected next bounded block after green main: **B07 Presentation/assets**.
