# 2026-09-06 — B02-04 social/player-agency semantics

## Цель

Закрепить без LLM смысл request/permission/response и закрыть T02–04 до scheduler/runtime.

## Сделано

- Добавлен strict `SocialAct` v1.0:
  - request;
  - permission;
  - response accept/refuse.
- Structured subject не содержит duration/effects/state mutation.
- `CalculatedAction` расширен social variants.
- Request → conditional/AWAITING_RESPONSE без effects.
- Permission → executed как permission speech act, но без исполнения permitted action.
- Response валидирует concrete proposalId и responder; accept/refuse остаются отдельными решениями без effects.
- Добавлены T02–T04 Core regressions.
- Generated capabilities/SKILL расширены social types; HTTP operations остались unavailable.
- Добавлен ADR 0006.

## CI

Первый run `34029050442` упал на TypeScript после честного расширения `CalculatedAction` union:

- paint helper был типизирован как `Omit<CalculatedAction,...>` и после union потерял paint-specific fields;
- exact-empty social effects вывелись как `readonly never[]`.

Исправление `be9c2d76f2b30252c72ed4044010f3623b1664eb`:

- paint helper/return narrowed до `PaintCalculatedAction`;
- social resolvers создают конкретные calculated variants и exact `readonly []`.

Финальный run `34029126259`:

- contract 28/28;
- Core 26/26;
- boundaries pass;
- generated docs current.

## Общая приёмка B02

Сверено с каноническим ТЗ. B02 требует typed conditions/effects, read-only inputs, trial effect batch, resolver, statuses executed/partial/conditional/blocked и матрицу T01–04/T07. Все требования имеют проверяемый код:

- T01 — B02-02;
- T02–04 — B02-04;
- T07 — B02-03.

Дополнительный B02-05 не нужен. Следующий архитектурный слой — B03 scheduler.

## Не сделано

Persisted pending social requests, NPC decision AI, clock commit, tasks/events/deadlines, Runtime API/storage, LLM и Studio.

## Следом

`docs/tasks/B03-01-clock-event-queue.md` — integer clock + ordered event queue + deterministic interval plan как первый bounded slice B03.
