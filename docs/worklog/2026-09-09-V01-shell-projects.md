# V01 — shell, проекты, токены, русские тексты, роли

Дата: 2026-09-09. Ветка: `feat/b13-acceptance-closure`, коммит `ffc5ebf`.
Механика авторизации не менялась.

## Что сделано

- Тема «Светлая мастерская»: токены (§4.1 ТЗ) в `apps/studio/styles.css`
  поверх существующей вёрстки, без слома layout.
- Первый экран — «Мои проекты» (`apps/studio/src/app.ts`, класс StudioApp):
  панель 64px, поиск, карточки 3/2/1 (название, число квестов, роль),
  пустое состояние («О чём будет квест?» + «Создать с ИИ» с честной подписью
  paint-профиля + «Начать с пустого»), модалка 480px, ID генерирует сервер,
  поля Technical ID нет.
- Каркас редактора: верх 60px («Проверить», «Играть», inline-rename, состояние
  сохранения), библиотека 240px со сворачиванием, правая 336px
  («Свойства»/«Соавтор»), нижняя панель проверки. Движок панелей не тронут.
- F04: revision/hash/ID — в «Дополнительно»; якорь `draft-meta` сохранён
  (требует tour-реестр B05-04).
- Провайдер ИИ по роли (`provider-settings.ts`, `access.ts`): не-владелец
  видит только статус, форма скрыта по умолчанию.

## Проверки

- `npx tsc -b` — exit 0.
- `node --test apps/studio/test/*.test.mjs` — 69/69 (7 новых V01:
  цикл создать/открыть без ID, токены, корневые пути, пустое состояние,
  карточки, модалка, каркас).
- По ходу: починены 2 регресса от частичной работы (якорь revision,
  пути onboarding в `studio-assets`); тесты не удалялись.
- VPS: образ `vps-studio` пересобран, `:8740/studio-assets/styles.css`
  содержит токен `176B56`, index 200; Player `playtest-2` переподнят
  (`/player-assets/app.js` 200).

## Исполнитель

Основную работу сделал субагент (фоновый), упал на ошибке провайдера без
финального отчёта; ветку довёл и проверил владелец сессии вручную.

## Границы/остаток

- Визуальный скриншот новой темы в живом браузере не снят (инструмент
  автоматизации висит) — за приёмкой с живым входом.
- Worklog V01 короткий (субагент свой не оставил).
- Профиль показывает роль+ID; настоящее имя из Telegram — только через
  аддитивное поле `/gate/check` (не сделано, отложить к командным ролям).
