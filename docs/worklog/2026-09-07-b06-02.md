# 2026-09-07 — B06-02 free-text intent boundary

## Base

Начато только после published B06-01:

- B06-01 merge: `a08f3434060abe699be5d431464597645557b8f9`;
- main push-CI: `34056977026` — success;
- branch: `b06-02-free-text-intent-boundary`;
- PR: #20.

## Цель slice

Свободный текст должен получить bounded understanding layer и после validation попасть в тот же Core resolver, что explicit action. Narrator и художественный текст не входят в B06-02.

## Реализация

### Intent boundary

Commit `414be66c6d7f471e1b37fe649f4aff0b42d01c16`:

- добавлен `IntentDecision` (`resolved` / `needs_clarification` / `unsupported` / `failed`);
- `ModelIntentInterpreter` — максимум 2 attempts с одним absolute deadline;
- exact-shape validator;
- action catalog / args / entity allowlists;
- original text пришивается к `ResolvedIntent.sourceInput` только после validation;
- authority fields и неизвестные mechanics отклоняются;
- T02/T04/T09/T16 prepared fake corpus;
- fake tests явно не считаются доказательством качества real Russian model.

### Runtime integration

Commit `6761c2df54f9e1e5f3760a2ad1c05790850b0b52`:

- общий Core helper для explicit/text intent;
- new text request shape при сохранении legacy explicit shape;
- request hash по исходному text до AI;
- `claimOperation` до interpreter;
- resolved text → существующий Core calculation;
- clarification/unsupported/failed → `finishWithoutTurn`;
- clarification reference/revision protection;
- Runtime intent integration tests.

### CI correction

Первый полный integration run `34057574708` дошёл до `test:server`: typecheck, contracts, core, AI (19/19), storage и control были green.

Он выявил 5 локальных regressions:

1. два старых T15 explicit tests получили 500 — explicit HTTP branch случайно использовал `executeIntent`, а legacy test wrapper реализует `execute`;
2. новый convergence test подтвердил ту же двойную маршрутизацию счётчиком;
3. два clarification tests получили 400 — optional input keys сравнивались с ошибочной sorted-position логикой.

Commit `9d21c3d7bc8c5b43f4caa32bbbc6438a4c0cc361`:

- explicit HTTP branch снова вызывает public `executor.execute`;
- внутри canonical executor `execute` всё равно преобразует command в `ResolvedIntent` и использует общий Core helper;
- clarification parser переведён на exact key-set validation;
- clarification ID привязан к persisted operation ID и проверяется против stored no-turn response/revision.

### Frozen binding

Commit `8e530be6232ce7cc2c0ed3c198eb84f6b44ba71e`:

- Player runtime template получает intent catalog рядом с exact frozen release/state;
- executor остаётся bound к frozen authored action definition;
- draft edit не переключает уже запущенный frozen path.

## Functional evidence

PR CI `34057719194` на head `8e530be6232ce7cc2c0ed3c198eb84f6b44ba71e` — **success**, полный `npm run verify`.

Проверено:

- AI strict boundary;
- explicit/text convergence;
- no duplicate provider/Core on replay;
- no-turn clarification/unsupported/failed;
- unchanged revision/time/resources for processing outcomes;
- stale/forged clarification rejection before provider/Core;
- injection cannot introduce privileged action/statePatch;
- old B04/B05/T15 behavior remains green;
- Player frozen binding remains green.

## Честная граница доказательства

Этот slice доказывает безопасный structural intent boundary и fake-provider behavior. Он **не доказывает**, что выбранная живая модель правильно понимает весь русский corpus. Полноценная semantic dialogue context orchestration также не объявляется готовой.

## Следующий gate

После ADR/STATUS/HANDOFF sync:

1. final current-head PR CI;
2. PR #20 ready;
3. merge с pinned expected head;
4. exact merge-SHA push CI;
5. только затем B06-02 published.

После publication — отдельный B06-03: narrator + FactPacket + strict/expressive + deterministic template fallback + combined deadline/failure semantics.
