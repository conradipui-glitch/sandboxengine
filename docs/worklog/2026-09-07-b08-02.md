# 2026-09-07 — B08-02 trusted backend plugin execution

Base: published B08-01 merge `375233479ab787a6215be7a1d6aab52ec9c47a74`, main CI `34105710910` success.

Branch: `b08-02-backend-plugin-execution-contracts`  
PR: #29

## Implemented

- added trusted executable registration contracts separate from plugin manifest data;
- added registry-driven action resolver and scheduled-event handler lookup;
- resolver input is detached/deep-frozen state + validated JSON args + simulation clock + bounded deterministic RNG;
- resolver output is exact bounded `PluginActionPlan` data only;
- host owns RNG provenance/state and catches resolver/validator failures;
- canonical gameplay effects only; custom effect/state patches fail closed;
- server action adapter applies plugin plan via existing Core effect/time gates;
- added generic plugin scheduler handler boundary;
- hardened public execution registry so `ReadonlyMap.forEach` cannot expose backing mutable Map;
- hardened blocked/partial plan semantics and generated-event source ownership;
- wired plugin scheduler handlers through Core `processTimeAdvancePlan` so Core owns dynamic queue validation;
- hardened boundary checker against Core→plugins dependency, DB/process/dynamic-code access and obvious wall-clock/entropy shortcuts in canonical plugin sources.

## Test / CI progression

1. `234ac18ffd160de6d147579df774c230775ab549` → CI `34106576923` success: generic execution boundary compatible with existing engine.
2. Added adversarial tests for detached state/args, invalid args, RNG provenance/limits, authority widening, custom effects and Core effect/time behavior.
3. `1e701a6ed509afa120b9b3157a4f9c7989afa756` → CI `34106751806`: one test failure from false assumption that adjacent seeds must produce different bounded RNG buckets; implementation remained green elsewhere.
4. Corrected RNG test and hardened registry/plan/event semantics.
5. `8b512837d50287460406b158081cceebb6d1c840` → CI `34107152241`: TypeScript found missing public `PluginActionResolutionResult` re-export.
6. `7154dfb5512b186728e2c8d6a25fea3ad9252f38` → CI `34107248938` success.
7. Post-green semantic audit found missing proof that plugin scheduler children entered Core dynamic scheduler.
8. `1b10aa7aaf25ae877f81575c192482a806b5a135` added `processRegisteredPluginSchedulerThroughCore` and regressions for valid child processing, past-child rejection, duplicate-child rejection and RNG non-publication on failure → CI `34108213347` success.
9. `a8cec6c6fc7e1721d5d9b965cb9e57c88040163a` hardened architecture/determinism boundary checks → CI `34108349316` success.

## Semantic result

Unresolved BLOCKER: **0**.

The important authority rule is now proven:

`trusted plugin callback -> bounded data plan/events -> existing Core effect/time/scheduler gates -> candidate state`

No plugin resolver receives commit authority or returns authoritative state. No plugin-specific branch was added to Core.

## Honest limitations

- trusted in-process code, not a third-party sandbox;
- no concrete installed plugin yet;
- no custom `WorldState` extension reducer;
- no public Runtime plugin endpoint;
- no Studio/UI plugin surface;
- release requirement enforcement is not wired to publish/start in this slice.

Next after verified publication: **B08-03 — dice-check proof plugin**.
