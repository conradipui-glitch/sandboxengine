# B05-02 — worklog 2026-09-06

## Цель

Собрать первый человеческий Studio authoring path поверх опубликованного B05-01 Control API без второй локальной quest truth.

## Реализовано

- branch `b05-02-minimal-studio-forms` от B05-01 main merge `07aacacb68178c119d11b555a8c166ebe65fe791`;
- PR #16;
- dependency-free TypeScript + DOM `apps/studio`;
- typed `ControlApiClient`;
- human forms project/quest/resource/bounded `core.paint`;
- cost edit через `block.replace` + `baseRevision`;
- validation revision/hash/status/errors;
- explicit stale-conflict review/retry/cancel;
- loopback Studio dev server + same-origin `/control/*` proxy;
- responsive 680px/mobile collapse, labels/focus styles;
- runnable `npm run dev:studio`;
- `test:studio` включён в root verify.

## Исправления по CI

### Run 34048457576

Failure: DOM `fetch` не принимал `Uint8Array<ArrayBufferLike>` как BodyInit. Request body proxy переведён на `ArrayBuffer`.

### Run 34048515006

Typecheck и основной author path прошли; Studio 3/4. Единственный failure: static root вычислялся относительно `dist/src`, поэтому `/` → 404. Root исправлен на каталог `apps/studio`.

### Run 34048587043

Success. Studio 4/4, все предыдущие suites/boundaries/docs green.

### Run 34048643611

После дополнительного smoke compiled browser entry — success.

## Доказанные invariants

- author path project → quest → resource → paint cost=1 → validation → cost=2 проходит через real SQLiteControlStore + Control HTTP + Studio proxy;
- новый API client/reload видит authoritative cost=2;
- stale save получает 409 и не мутирует server draft;
- retry происходит только отдельным запросом на fresh revision;
- Studio proxy не принимает non-loopback Control origin;
- compiled browser entry реально отдаётся сервером;
- public API/registry не изменялись, generated docs остаются текущими.

## Осознанная граница

Browser automation/Playwright не добавлялись: в репозитории не было browser tooling, а B05-02 не требует dependency/tooling expansion для доказательства authoring semantics. UI не объявляется финально отполированным дизайном; визуальная система намеренно минимальна.

## Следующее

После final docs gate → merge PR #16 → push-CI main перейти к B05-03: basic Player + frozen playtest gameplay E2E.
