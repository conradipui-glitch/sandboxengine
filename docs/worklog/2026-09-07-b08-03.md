# 2026-09-07 — B08-03 dice-check proof plugin

## Base

Started exactly from published B08-02 merge `a3eef28d106d89bdc4e05cf5c9fd7c1eb61e97e1`; exact main CI `34108753710` success.

Branch: `b08-03-dice-check-proof-plugin`.  
PR: #30.

## Implemented

- real installed `dice-check@1.0.0` manifest;
- capability/action/schema/recipe/block metadata;
- strict plugin-owned skill-check schema and runtime validator;
- schema-driven Studio form metadata;
- bounded text narrative recipe/projector;
- deterministic one-d20 resolver through the generic B08-02 RNG capability;
- authored success/failure canonical effect branches;
- runtime args limited to authored `definitionId`;
- server proof through generic plugin host -> Core effects/time;
- generic artifact plugin-requirements sidecar/preflight;
- generated agent docs updated from zero to one installed plugin.

## CI / iteration evidence

- `acdce4946193a67407723a49c9ca6cada7226d13` → CI `34109914329`: four primary TypeScript defects in new `dice-check.ts`;
- `d913d7b38676b00a1f209ef31e0be21c11a5a0d6` → CI `34110050259`: all functional suites and boundary checks green; only generated docs stale;
- generator/docs sync head `8c23a3358e5783ee2b4d998d6ae525d478b0f7d2` → CI `34112482535` success.

## Post-green semantic finding

Audit found one real blocker: plugin requirements were artifact-hash-bound, but executable `DiceCheckDefinition[]` were still independently supplied. That allowed authored mechanics to drift from the frozen artifact identity.

Hardening added:

- `DiceCheckAuthoredSidecar`;
- exact artifact-hash validation before registration;
- detached/deep-frozen authored definitions;
- revalidation/cloning when materializing the registration;
- plugin and server regressions for mutation/hash mismatch/exact-bound execution.

Hardening head `339b01b99ea70d8e2b9db958384d95be66598f8b` → CI `34112797727` success.

## Acceptance

B08-03 semantic audit: unresolved BLOCKER = 0.  
Canonical B08 audit: unresolved BLOCKER = 0.

## Honest boundary

No public generic plugin endpoint, custom WorldState reducer, full Studio plugin editor, custom result widget, dynamic remote plugin loading or untrusted-code sandbox was added. B09 owns production auth/publish persistence and enforcement of the B08 sidecars.

## Next

Finish Publication Gate on exact docs/current head: final CI -> PR #30 ready -> pinned merge -> exact merge-SHA push-to-main CI. Only after that call B08 published and start B09 exactly from the verified B08 merge SHA.
