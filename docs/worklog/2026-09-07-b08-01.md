# Worklog — 2026-09-07 — B08-01

## Goal

Create the first plugin/extension foundation without executing plugin gameplay code.

## Base

Started exactly from published canonical B07 merge `1889145e1784e9186d0914207168448c111b8114` after exact main CI `34102992250` succeeded.

## Implementation

Added `@living-history/plugins` with:

- data-only versioned plugin manifest;
- namespaced block/action/event/effect/capability/recipe/UI/schema identifiers;
- engine plugin API range checks;
- deterministic dependency graph validation and ordering;
- global owned-ID collision detection;
- frozen registry snapshot;
- pure release compatibility checker.

Added Draft 2020-12 `plugin-manifest.schema.json` and build registry metadata at `packages/plugins/registry/installed.json`.

The installed registry intentionally contains zero plugins in B08-01.

Root verification now includes `test:plugins`; boundary checks reject gameplay imports, network/process/filesystem/dynamic-code authority in the registry package.

Generated agent contracts now expose plugin manifest/API versions, actually installed trusted plugin metadata/capabilities and a separate plugin registry hash. No resolver execution or `dice-check` is advertised.

## CI / hardening

First implementation head: `f4ea4bca1f5fb170db81b456cf9c29de7abf00dd`.

CI `34103718389` failed only at TypeScript compile because the successful compatibility result used `readonly never[]` instead of the exact empty tuple type.

Type fix: `3bd15d318de0c94e5038a1af3f44148de794bfe0`.

CI `34104110358` succeeded across the full repository.

Post-green hardening added the canonical manifest JSON Schema, installed registry metadata and plugin-aware generated docs.

Hardening/docs head: `adee3d4b2328d5fad5f56df9a347450d875fb496`.

CI `34105282304` succeeded, including plugin tests, old B01–B07 regressions, boundary checks and exact `docs:check`.

Semantic audit result: unresolved `BLOCKER = 0`.

## Scope deliberately deferred

- resolver/event/effect execution;
- Runtime integration of plugin actions;
- Studio forms;
- UI plugin rendering;
- `dice-check` proof plugin;
- marketplace/sandbox;
- B09 auth/publish.

## Next

Complete B08-01 Publication Gate. Then create B08-02 exactly from the verified B08-01 merge SHA.
