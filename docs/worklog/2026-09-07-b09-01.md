# Worklog — 2026-09-07 — B09-01

## Goal

Replace loopback secrecy as the only Control protection with explicit closed-user authentication, durable sessions, CSRF and project roles while preserving local loopback-owner development.

## Implementation

- Added bounded Control security types/helpers.
- Added scrypt password verifier with random salt and timing-safe compare.
- Added opaque random session/CSRF secrets with hash-at-rest storage contract.
- Added Memory and SQLite security stores.
- Added durable users, sessions/revoke, memberships and last-owner protection.
- Made authenticated project creation + owner membership atomic in SQLite.
- Added login/session/logout HTTP endpoints.
- Added owner-only members list/update/delete endpoints.
- Added safe frozen-playtest read route for project members.
- Centralized project role enforcement across existing Control routes.
- Added process-local login throttle with injected service time.
- Added Origin allowlist, credentialed CORS/preflight and non-loopback secure-cookie gate.
- Kept local loopback-owner as an explicit separate mode.
- Wired standalone server authenticated mode from operator environment configuration and closed-user provisioning.
- Updated endpoint registry and generated agent/OpenAPI capabilities; publish/rollback remains absent.

## CI / hardening history

- storage foundation `29990a3e554ba0485beb33a3fa5707a4e56cbe94` → CI `34114308237` success;
- first HTTP head `5651614ff6ba89871c62c80b48d6f2d8f78ec284` → CI `34118452858` failed only on throttle expectation (`401` vs expected `429` on threshold attempt);
- corrected regression `ac96191e1ac8ab2f76014bb124ddb83a5363d02d` → CI `34118625979` success;
- endpoint/standalone wiring made generated docs stale as expected;
- semantic audit found browser CORS/preflight blocker;
- hardening `c678a3493de7af473fd88640a40968c74456fd91` → all functional suites green; CI `34119305168` failed only `docs:check`;
- generated contract sync `f8e5263ae32e0c3582d99f6126d11aad2653d647` → CI `34119808138` success.

## Acceptance

B09-01 semantic unresolved BLOCKER = **0**.

Next after verified publication: B09-02 immutable release build + owner-only publish/rollback with exact validation/content hash/B08 plugin-sidecar preflight.