# 2026-09-06 — B01-03 authoring schemas

## Контекст

B01-02 опубликован в `main` merge commit `270c98bd272dbe43879fb023df6e01f647782d35`, push CI `34022088748` прошёл. B01-03 продолжает только контрактный каркас: авторские blocks/release/intent и один целостный fixture-пакет. Resolver/runtime/LLM не входят.

## Сделано

- Добавлены schema IDs и JSON Schema v1.0 для `Block`, `QuestRelease`, `ResolvedIntent`.
- Первый Block registry: `core.location`, `core.character`, `core.resource`; `data` каждого kind имеет отдельный строгий shape.
- `QuestRelease` фиксирует quest/release IDs, compatibility, полный список block IDs и entry location.
- `ResolvedIntent` фиксирует actionType, participant/target IDs, JSON args и исходный text/action input, но не содержит результата исполнения.
- Добавлен semantic validator package integrity: duplicate IDs, missing block refs, entry/character locations и resource min/initial/max.
- Создан `minimal-quest` fixture-пакет из workshop, painter, blue-paint и release manifest.
- Добавлены отрицательные fixtures на unknown kind, wrong version, missing ref, duplicate ID и forbidden mutation fields intent.

## Проверки

GitHub Actions PR run [34022346033](https://github.com/conradipui-glitch/sandboxengine/actions/runs/34022346033):

- Node `24.19.0`, npm `11.17.0`;
- `npm ci` — success;
- `npm run verify` — success;
- contracts: 16 passed, 0 failed;
- core: 2 passed, 0 failed;
- boundary check — success;
- docs check — success.

## Наблюдения

- `npm ci` снова сообщил 2 vulnerabilities (1 moderate, 1 high); dependency investigation не смешивался с contract slice.
- GitHub-hosted actions продолжают предупреждать о deprecated Node 20 target внутри pinned checkout/setup-node; workflow фактически выполняется на Node 24 и успешен.

## Не сделано

B01 ещё не закрыт: требуется B01-04 с compile skeleton/hash, readiness registry, reproducible generated docs/contracts и вторым fixture-пакетом. Никакие T01–T37, runtime handlers, AI или Studio этим change set не приняты.
