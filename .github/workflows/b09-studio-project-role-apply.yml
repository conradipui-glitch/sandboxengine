name: B09 Studio Project Role Apply

on:
  push:
    branches:
      - b09-03-version-history-studio-access

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: b09-03-version-history-studio-access
      - run: python scripts/b09-studio-project-role-sync.py
      - shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/server/src/control-server.ts
          git rm scripts/b09-studio-project-role-sync.py .github/workflows/b09-studio-project-role-apply.yml
          git diff --cached --quiet && exit 0
          git commit -m "feat(B09-03): expose current project role in project views"
          git push origin HEAD:b09-03-version-history-studio-access
