name: B09 Studio Release Build Apply

on:
  push:
    branches:
      - b09-03-version-history-studio-access

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: b09-03-version-history-studio-access
          fetch-depth: 0
      - name: Apply release build patch
        run: python scripts/b09-studio-release-build-sync.py
      - name: Remove staging files
        run: |
          rm scripts/b09-studio-release-build-sync.py
          rm .github/workflows/b09-studio-release-build-apply.yml
      - name: Commit applied patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat(B09-03): add Studio release build and publish report"
          git push origin HEAD:b09-03-version-history-studio-access
